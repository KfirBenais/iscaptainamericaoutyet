# מחשבון תמחור פנימי

דף פרטי לחישוב מחיר להדפסה. **לא מקושר משום מקום באתר** — לא בתפריט, לא בפוטר, לא בקטלוג.

## הכתובת

```
https://<הדומיין-שלך>/x/27d16ba9b5ac9e6d
```

עובד גם כ-hash route, למקרה שהאחסון לא מפנה נתיבים עמוקים ל-`index.html`:

```
https://<הדומיין-שלך>/#/x/27d16ba9b5ac9e6d
```

לשינוי הכתובת (רוטציה) — ערוך את `SECRET_PATH` בקובץ `src/pricingConfig.js`.

כדי ליצור מזהה אקראי חדש:

```bash
node -e "console.log(require('crypto').randomBytes(8).toString('hex'))"
```

## מה נוסף לפרויקט

| קובץ | תפקיד |
|------|-------|
| `src/pricingConfig.js` | **כל הקבועים והנתיב הסודי** — כאן עורכים מחירים |
| `src/pricingFormula.js` | נוסחת החישוב (פונקציה טהורה) |
| `src/pricingFormula.test.js` | בדיקות לנוסחה |
| `src/PricingCalculator.js` | הדף עצמו + שער הסיסמה |
| `src/PricingCalculator.css` | עיצוב הדף |
| `src/index.js` | ניתוב: הנתיב הסודי מרנדר את המחשבון, כל השאר את האתר |
| `public/_redirects` | כלל SPA של Netlify, כדי שהנתיב המלא יעבוד ולא יחזיר 404 |

## הנוסחה

| רכיב | חישוב | ברירת מחדל |
|------|-------|-------------|
| חומר | (גרם ÷ 1000) × מחיר לק״ג | 60 ₪ לק״ג |
| דמי טיפול | קבוע לפריט (הכנה, הסרה מהמדפסת, ניקוי בייס) | 8 ₪ |
| זמן מדפסת | שעות × תעריף שעתי (חשמל + בלאי) | 2 ₪ לשעה |
| הדבקה | לפי רמה | 0 / 5 / 10 / 15 ₪ |
| צביעה | לפי רמה | 0 / 10 / 20 / 30 ₪ |
| **מחיר סופי** | סכום הרכיבים, מעוגל | ל-5 ₪ הקרובים |

שתי דרכים לשנות את המספרים:

1. **קבוע** — עריכת `PRICING_DEFAULTS` ב-`src/pricingConfig.js` (דורש דיפלוי).
2. **זמני** — פאנל "⚙️ קבועי תמחור" בתחתית הדף. השינויים משפיעים על המחיר מיד ונשמרים ב-`localStorage` של הדפדפן. כפתור "שחזר ברירות מחדל" מחזיר לערכים שבקובץ.

## הגנת סיסמה (אופציונלי)

השער מופעל אוטומטית ברגע שאחד ממשתני הסביבה קיים. אם אף אחד מהם לא מוגדר — הדף נפתח ישר, בלי סיסמה.

**מומלץ — הסיסמה עצמה לא נכנסת לקוד.** מייצרים ממנה SHA-256:

```bash
node -e "console.log(require('crypto').createHash('sha256').update('הסיסמה-שלך').digest('hex'))"
```

ואת הפלט שמים במשתנה:

```
REACT_APP_PRICING_PASSWORD_SHA256=<הפלט של הפקודה>
```

קיימת גם חלופה בטקסט גלוי, `REACT_APP_PRICING_PASSWORD=...`, אבל אז הסיסמה נמצאת כמות שהיא בקוד שנשלח לדפדפן — עדיף להימנע.

**איפה מגדירים:**

- **בפרודקשן (Netlify):** Site settings → Environment variables. אחרי ההוספה צריך deploy מחדש — משתני `REACT_APP_*` נצרבים לקוד בזמן ה-build, לא בזמן ריצה.
- **בפיתוח מקומי:** בקובץ `.env.local`. **לא** בקובץ `.env` — הוא נמצא ב-git ויעלה לריפו.

הסיסמה נשמרת ב-`sessionStorage`, כך שריענון דף לא מבקש אותה שוב, אבל סגירת הטאב כן.

### מה השער הזה באמת נותן

חשוב שתדע במדויק מה קנית, כי זה לא מה שנראה במבט ראשון:

- ✅ **מונע כניסה מקרית.** מי שנתקל בכתובת לא יראה את המחירים.
- ✅ **הסיסמה עצמה לא דולפת** (בגרסת ה-SHA-256) — היא לא מופיעה בקוד שנשלח לדפדפן. זה חשוב אם אותה סיסמה משמשת אותך במקום אחר.
- ❌ **לא עוצר מישהו שבאמת מנסה.** האתר הוא React סטטי — כל הבדיקה רצה בדפדפן של המבקר. גם הנתיב הסודי וגם קוד המחשבון נמצאים בתוך קובץ ה-JS הראשי, ומי שיודע לקרוא אותו יכול למצוא את שניהם ולעקוף את השער. אין שרת שיכול למנוע את זה.

כלומר: הנתיב הסודי והסיסמה הם וילון, לא מנעול. לצורך מחשבון תמחור אישי זה בדרך כלל מספיק לגמרי.

**אם בהמשך תרצה מנעול אמיתי** — הכי פשוט זה Netlify Password Protection (Site settings → Access control → Password protection), שחוסם ברמת השרת עוד לפני שהקוד מגיע לדפדפן. הוא בתוכניות בתשלום. חלופה: להעביר את החישוב ל-Netlify Function שמאומתת בצד השרת.

## למה הדף לא מופיע ב-robots.txt

הוספת הנתיב ל-`robots.txt` הייתה **מפרסמת** אותו — הקובץ הזה פתוח לכולם בכתובת `/robots.txt`. במקום זה הדף מוסיף לעצמו בזמן טעינה תגית:

```html
<meta name="robots" content="noindex, nofollow, noarchive">
```

בתוספת לכך שאין אליו שום קישור, אין דרך סבירה שמנוע חיפוש יגיע אליו.

## בדיקות

```bash
npm test -- --testPathPattern=pricingFormula --watchAll=false
```
